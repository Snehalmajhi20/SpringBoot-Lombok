# SpringBoot-Lombok
By using lombok we skips the some business logic multiple times the developers write boilerplate codes to get same results like getter setter methods , ToString, Constructor, Hashcode, Equals etc -by annoation we can skip this methods.
